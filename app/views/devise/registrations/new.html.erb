<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      
      <h1></h1>
      
      <h2>Sign Up</h2>
      
      <% if params[:plan] == '2' %>
        <%= render "pro_form" %>
      <% else %>
        <%= render "basic_form" %>
      <% end %>
      
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>