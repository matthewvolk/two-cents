<div class="home-callout text-center container-fluid">
    <h1><i class="fa fa-hand-scissors-o" aria-hidden="true"></i> TwoCents</h1>
    <hr/>
    <h4>A subscription-membership software product, by Matthew Volk</h4>
</div>
  <div class="card-deck">
    <div class="container">
        <div class="row">
      <div class="col-sm-6">
        <div class="card text-center">
          <div class="card-body">
            <h4 class="card-title">Basic Membership</h4>
            <hr />
            <p class="card-text">The button below will pass basic plan vars and params
            through the string query taking the user to the regular registration path.</p>
            <%= link_to "Start for Free", new_user_registration_path(plan: @basic_plan.id), class: "btn btn-primary" %>
          </div>
        </div>
      </div>
      <div class="col-sm-6">
        <div class="card text-center">
          <div class="card-body">
            <h4 class="card-title">Pro Membership</h4>
            <hr />
            <p class="card-text">The button below will pass pro plan vars and params
            through the string query taking the user to the premium registration path.</p>
            <%= link_to "$10/month", new_user_registration_path(plan: @pro_plan.id), class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </div>
    </div>
  </div>


<!-- /saasapp/public/index is for splash pages, won't register users
     /saasapp/app/views/pages/index is rails functional -->